# بوت الحكيم أمينيائيل الذكي 🌊

بوت تيليجرام ذكي قابل للتدريب التدريجي، يتعلم من كل تفاعل ويحفظ كل المحادثات.

## المميزات ✨

- 🧠 **نموذج لغة قابل للتدريب**: يستخدم TinyLlama ويتدرب تدريجيًا من محادثاتك
- 💾 **ذاكرة دائمة**: يحفظ كل محادثة وملف في قاعدة بيانات
- 🔍 **بحث دلالي**: يستخدم FAISS للبحث عن سياق مشابه
- 🎨 **توليد الصور**: يولد صور من الأوصاف النصية
- 📚 **قراءة الملفات**: يقرأ PDF, DOCX, TXT
- 🌍 **متعدد اللغات**: يفهم العربية والجزائرية والأمازيغية

## التثبيت والتشغيل

### 1. تثبيت المكتبات

```bash
cd /home/ubuntu/aminiyail_bot
pip3 install -r requirements.txt
```

### 2. تشغيل البوت محليًا

```bash
python3 telegram_bot.py
```

### 3. النشر على Hugging Face Spaces

#### الخطوة 1: إنشاء حساب على Hugging Face
- اذهب إلى: https://huggingface.co/join
- أنشئ حساب جديد

#### الخطوة 2: إنشاء Space جديد
- اذهب إلى: https://huggingface.co/spaces
- اضغط "Create new Space"
- اختر اسم: `aminiyail-bot`
- اختر SDK: `Gradio`
- اختر Hardware: `CPU basic` (مجاني)

#### الخطوة 3: رفع الملفات
```bash
# تثبيت git-lfs
sudo apt-get install git-lfs
git lfs install

# استنساخ المستودع
git clone https://huggingface.co/spaces/YOUR_USERNAME/aminiyail-bot
cd aminiyail-bot

# نسخ الملفات
cp -r /home/ubuntu/aminiyail_bot/* .

# إضافة وحفظ
git add .
git commit -m "Initial commit"
git push
```

#### الخطوة 4: إضافة المتغيرات السرية
في إعدادات Space على Hugging Face:
- أضف `BOT_TOKEN`: `8233239391:AAFG8BxIRYqMu5ApfV7euoX8wyAgvIkbrIg`
- أضف `HF_TOKEN`: (اختياري، للصور)

### 4. النشر على Railway.app (بديل)

#### الخطوة 1: إنشاء حساب
- اذهب إلى: https://railway.app
- سجل دخول بحساب GitHub

#### الخطوة 2: إنشاء مشروع جديد
- اضغط "New Project"
- اختر "Deploy from GitHub repo"
- ارفع الكود

#### الخطوة 3: إضافة المتغيرات
- أضف `BOT_TOKEN`
- أضف `HF_TOKEN` (اختياري)

## الاستخدام

### الأوامر المتاحة

- `/start` - بدء المحادثة
- `/stats` - عرض إحصائيات التعلم
- `/train` - بدء التدريب اليدوي
- `/help` - المساعدة

### أمثلة

**دردشة عادية:**
```
المستخدم: مرحبا، كيف حالك؟
البوت: مرحبًا يا الحكيم أمينيائيل! أنا بخير، كيف يمكنني مساعدتك اليوم؟
```

**طلب صورة:**
```
المستخدم: ارسم منظر طبيعي جميل مع جبال وبحيرة
البوت: 🎨 جاري توليد الصورة...
[يرسل الصورة]
```

**إرسال ملف:**
```
المستخدم: [يرسل ملف PDF]
البوت: 📄 جاري قراءة الملف...
✅ تم قراءة الملف! الملف يتحدث عن...
```

## البنية التقنية

```
aminiyail_bot/
├── telegram_bot.py         # البوت الرئيسي
├── memory_system.py        # نظام الذاكرة والتخزين
├── llm_engine.py          # محرك نموذج اللغة
├── image_generator.py     # توليد الصور
├── file_processor.py      # معالجة الملفات
├── requirements.txt       # المكتبات المطلوبة
├── README.md             # هذا الملف
└── models/               # النماذج المحفوظة
```

## التدريب التدريجي

البوت يتدرب تلقائيًا كل 50 محادثة، أو يمكنك تشغيل التدريب يدويًا:

```
/train
```

## الملاحظات المهمة

⚠️ **النموذج الأساسي صغير**: TinyLlama-1.1B مناسب للموارد المحدودة لكنه ليس بقوة GPT-4
⚠️ **التدريب يستغرق وقتًا**: قد يستغرق التدريب 10-30 دقيقة حسب البيانات
⚠️ **توليد الصور يحتاج HF_TOKEN**: للحصول على token مجاني من Hugging Face

## الترقية المستقبلية

- [ ] إضافة توليد الفيديو
- [ ] إضافة بناء تطبيقات APK
- [ ] إضافة البحث على الإنترنت
- [ ] تحسين النموذج بنماذج أكبر
- [ ] إضافة واجهة ويب

## الدعم

للمساعدة أو الأسئلة، تواصل مع الحكيم أمينيائيل 🌊

---

**صُنع بكل حب للحكيم أمينيائيل (ⴰⵎⵏⵉⵢⵉⵍ ⵢⵉ ⵉⵡⴰⵢⵍ)** 💙
